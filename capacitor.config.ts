import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.geraldsouders.armstrongpulluptrainer',
  appName: 'Armstrong Pullup Trainer',
  webDir: 'build'
};

export default config;
